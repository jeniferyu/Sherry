import SwiftUI

// LeafDetailSheet is no longer used after the Prayer Tree simplification.
// Kept as an empty placeholder to avoid breaking the Xcode project file.
struct LeafDetailSheet: View {
    var body: some View { EmptyView() }
}
